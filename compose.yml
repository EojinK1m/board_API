volumes:
  board-pgdata:

services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: "admin"
    ports:
      - 5432:5432
    volumes:
      - board-pgdata:/var/lib/postgresql/data
