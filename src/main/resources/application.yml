 spring:
  application:
    name: board

  jpa:
    database: postgresql
    open-in-view: false
    hibernate:
      ddl-auto: create

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: admin
